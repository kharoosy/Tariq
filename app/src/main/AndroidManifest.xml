<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Storage permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <!-- WhatsApp -->
        <package android:name="com.whatsapp" />
        <!-- WhatsApp Business -->
        <package android:name="com.whatsapp.w4b" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tariq">

        <!-- Main screen: browse and select images -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Sticker pack detail / send to WhatsApp screen -->
        <activity
            android:name=".StickerPackActivity"
            android:exported="false" />

        <!--
            ContentProvider queried by WhatsApp to retrieve sticker pack info.
            The authority MUST match the applicationId + ".stickercontentprovider".
        -->
        <provider
            android:name=".StickerContentProvider"
            android:authorities="com.tariq.stickermaker.stickercontentprovider"
            android:exported="true"
            android:readPermission="com.whatsapp.permission.STICKER_DATA" />

    </application>

</manifest>
